```html
<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Athena Hub</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

<style>
:root{
  --bg:#06070a;
  --card:#101217;
  --border:rgba(255,255,255,.09);
  --text:#f5f7fb;
  --muted:#8b91a1;
  --blue:#3b82f6;
  --cyan:#7dd3fc;
  --glow:rgba(59,130,246,.45);
}

*{
  margin:0;
  padding:0;
  box-sizing:border-box;
}

html{
  scroll-behavior:smooth;
}

body{
  min-height:100vh;
  background:
    radial-gradient(circle at 50% -10%,rgba(59,130,246,.18),transparent 35%),
    var(--bg);
  color:var(--text);
  font-family:Inter,sans-serif;
  overflow-x:hidden;
}

/* PARTICULES */

.particles{
  position:fixed;
  inset:0;
  pointer-events:none;
  overflow:hidden;
  z-index:0;
}

.particle{
  position:absolute;
  width:3px;
  height:3px;
  border-radius:50%;
  background:#7dd3fc;
  box-shadow:0 0 12px #3b82f6;
  opacity:.5;
  animation:float linear infinite;
}

@keyframes float{
  from{
    transform:translateY(110vh);
    opacity:0;
  }
  15%{opacity:.6}
  85%{opacity:.6}
  to{
    transform:translateY(-10vh);
    opacity:0;
  }
}

/* HEADER */

header{
  position:relative;
  z-index:1;
  text-align:center;
  padding:100px 20px 85px;
  border-bottom:1px solid var(--border);
  overflow:hidden;
}

.glow{
  position:absolute;
  width:700px;
  height:500px;
  left:50%;
  top:-300px;
  transform:translateX(-50%);
  background:radial-gradient(circle,var(--glow),transparent 70%);
  filter:blur(20px);
  animation:pulse 5s ease-in-out infinite;
}

@keyframes pulse{
  0%,100%{
    opacity:.55;
    transform:translateX(-50%) scale(1);
  }
  50%{
    opacity:1;
    transform:translateX(-50%) scale(1.15);
  }
}

.eyebrow{
  position:relative;
  color:var(--cyan);
  font-size:12px;
  letter-spacing:.18em;
  text-transform:uppercase;
  font-weight:700;
  margin-bottom:20px;
  animation:fadeUp .8s ease both;
}

.brand{
  position:relative;
  font-family:"Space Grotesk",sans-serif;
  font-size:clamp(45px,9vw,78px);
  font-weight:700;
  letter-spacing:.03em;
  background:linear-gradient(
    90deg,
    #fff,
    var(--cyan),
    var(--blue),
    #fff
  );
  background-size:300%;
  -webkit-background-clip:text;
  color:transparent;
  animation:
    fadeUp .8s .1s ease both,
    shine 5s linear infinite;
}

@keyframes shine{
  to{
    background-position:-300% center;
  }
}

.tagline{
  position:relative;
  max-width:480px;
  margin:16px auto 0;
  color:var(--muted);
  font-size:15px;
  animation:fadeUp .8s .2s ease both;
}

@keyframes fadeUp{
  from{
    opacity:0;
    transform:translateY(25px);
  }
  to{
    opacity:1;
    transform:translateY(0);
  }
}

/* MAIN */

.container{
  position:relative;
  z-index:1;
  max-width:760px;
  margin:auto;
  padding:75px 20px 100px;
}

.section{
  display:flex;
  align-items:center;
  gap:14px;
  color:var(--muted);
  font-size:11px;
  font-weight:700;
  letter-spacing:.18em;
  text-transform:uppercase;
  margin-bottom:28px;
}

.section:after{
  content:"";
  height:1px;
  flex:1;
  background:var(--border);
}

/* CARD */

.produit{
  position:relative;
  background:linear-gradient(
    145deg,
    rgba(255,255,255,.055),
    rgba(255,255,255,.015)
  );
  border:1px solid var(--border);
  border-radius:20px;
  padding:42px;
  overflow:hidden;
  animation:cardIn .9s .3s ease both;
  transition:.35s ease;
}

@keyframes cardIn{
  from{
    opacity:0;
    transform:translateY(35px) scale(.97);
  }
  to{
    opacity:1;
    transform:translateY(0) scale(1);
  }
}

.produit:before{
  content:"";
  position:absolute;
  width:250px;
  height:250px;
  right:-120px;
  top:-120px;
  background:radial-gradient(circle,var(--glow),transparent 70%);
  pointer-events:none;
}

.produit:hover{
  transform:translateY(-7px);
  border-color:rgba(59,130,246,.5);
  box-shadow:
    0 25px 80px rgba(0,0,0,.5),
    0 0 40px rgba(59,130,246,.12);
}

.produit h2{
  position:relative;
  font-family:"Space Grotesk",sans-serif;
  font-size:26px;
  margin-bottom:10px;
}

.desc{
  position:relative;
  color:var(--muted);
  font-size:15px;
  line-height:1.7;
  margin-bottom:25px;
}

.prix{
  position:relative;
  font-family:"Space Grotesk",sans-serif;
  font-size:38px;
  font-weight:700;
  margin-bottom:30px;
}

.prix span{
  color:var(--muted);
  font-size:13px;
  font-weight:500;
}

/* BUTTONS */

.actions{
  position:relative;
  display:flex;
  gap:12px;
  flex-wrap:wrap;
}

.btn{
  position:relative;
  border:0;
  border-radius:10px;
  padding:15px 24px;
  font-size:14px;
  font-weight:700;
  cursor:pointer;
  text-decoration:none;
  display:inline-flex;
  justify-content:center;
  align-items:center;
  transition:.25s ease;
  overflow:hidden;
}

.btn:after{
  content:"";
  position:absolute;
  top:0;
  left:-100%;
  width:60%;
  height:100%;
  background:linear-gradient(
    90deg,
    transparent,
    rgba(255,255,255,.45),
    transparent
  );
  transform:skewX(-20deg);
  transition:.6s;
}

.btn:hover:after{
  left:140%;
}

.btn-paypal{
  background:#ffc439;
  color:#003087;
  box-shadow:0 8px 30px rgba(255,196,57,.18);
}

.btn-paypal:hover{
  transform:translateY(-3px) scale(1.02);
  box-shadow:0 12px 35px rgba(255,196,57,.35);
}

.btn-virement{
  background:#2563eb;
  color:white;
  box-shadow:0 8px 30px rgba(37,99,235,.2);
}

.btn-virement:hover{
  transform:translateY(-3px);
  box-shadow:0 12px 35px rgba(37,99,235,.4);
}

/* VIREMENT */

.infos-virement{
  max-height:0;
  opacity:0;
  overflow:hidden;
  margin-top:0;
  padding:0 22px;
  background:rgba(59,130,246,.05);
  border:1px solid transparent;
  border-radius:12px;
  transition:.5s ease;
}

.infos-virement.open{
  max-height:300px;
  opacity:1;
  margin-top:24px;
  padding:22px;
  border-color:rgba(59,130,246,.2);
}

.infos-virement strong{
  display:block;
  color:var(--cyan);
  margin-bottom:12px;
  font-size:12px;
  letter-spacing:.12em;
}

.ligne{
  display:flex;
  justify-content:space-between;
  gap:20px;
  padding:9px 0;
  border-bottom:1px dashed rgba(255,255,255,.08);
  font-size:13px;
  color:#cbd0d9;
}

.ligne:last-child{
  border:0;
}

/* FOOTER */

footer{
  position:relative;
  z-index:1;
  border-top:1px solid var(--border);
  text-align:center;
  padding:35px 20px;
  color:var(--muted);
  font-size:12px;
}

/* MOBILE */

@media(max-width:600px){

  header{
    padding:75px 20px 65px;
  }

  .container{
    padding:60px 18px 80px;
  }

  .produit{
    padding:28px 22px;
  }

  .actions{
    flex-direction:column;
  }

  .btn{
    width:100%;
  }

  .ligne{
    flex-direction:column;
    gap:3px;
  }
}
</style>
</head>

<body>

<div class="particles" id="particles"></div>

<header>
  <div class="glow"></div>

  <div class="eyebrow">
    Boutique en ligne
  </div>

  <div class="brand">
    ATHENA HUB
  </div>

  <p class="tagline">
    Des produits pensés avec soin, un paiement simple et sécurisé.
  </p>
</header>

<main class="container">

  <div class="section">
    Nos produits
  </div>

  <div class="produit">

    <h2>Nom du produit</h2>

    <p class="desc">
      Description courte et soignée du produit ou du service que tu proposes à tes clients.
    </p>

    <div class="prix">
      29,99 <span>EUR</span>
    </div>

    <div class="actions">

      <!-- TON LIEN PAYPAL -->
      <a
        class="btn btn-paypal"
        href="https://www.paypal.com/paypalme/athenahubscript/29.99"
        target="_blank"
        rel="noopener noreferrer"
      >
        Payer avec PayPal
      </a>

      <button
        class="btn btn-virement"
        onclick="document.getElementById('virement1').classList.toggle('open')"
      >
        Payer par virement
      </button>

    </div>

    <div class="infos-virement" id="virement1">

      <strong>
        Coordonnées bancaires
      </strong>

      <div class="ligne">
        <span>IBAN</span>
        <span>FRXX XXXX XXXX XXXX XXXX XXXX XXX</span>
      </div>

      <div class="ligne">
        <span>BIC</span>
        <span>XXXXXXXX</span>
      </div>

      <div class="ligne">
        <span>Référence</span>
        <span>Votre nom + Commande</span>
      </div>

    </div>

  </div>

</main>

<footer>
  © 2026 Athena Hub · Tous droits réservés
</footer>

<script>

/* PARTICULES */

const container = document.getElementById("particles");

for(let i = 0; i < 45; i++){

  const particle = document.createElement("div");

  particle.className = "particle";

  particle.style.left = Math.random() * 100 + "%";

  particle.style.animationDuration =
    (7 + Math.random() * 12) + "s";

  particle.style.animationDelay =
    (-Math.random() * 15) + "s";

  const size = 1 + Math.random() * 3;

  particle.style.width = size + "px";
  particle.style.height = size + "px";

  container.appendChild(particle);
}

</script>

</body>
</html>
```
